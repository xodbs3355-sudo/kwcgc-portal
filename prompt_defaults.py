"""
서류별 검토 프롬프트 기본값.

이 파일은 "공식 기본값" — 관리자가 따로 설정 안 한 서류에 적용됩니다.
관리자 페이지에서 수정한 내용은 `data/prompts.json` (Railway: /data/prompts.json)에
저장되며 그쪽이 우선 적용됩니다.

프롬프트 내 `{project_info}` 자리에 사용자가 입력한 공사 정보가 자동 삽입됨.

수정 방법:
- 이 파일을 편집 → git push → Railway 자동 배포
- 관리자가 이미 저장한 프롬프트가 있는 서류는 영향 받지 않음
  (관리자가 "기본값으로 복원" 누르면 그 시점의 이 파일 내용을 가져감)

PROMPTS 딕셔너리에 없는 doc_id는 generic 기본 템플릿이 자동 적용됨.
"""

PROMPTS = {
    # ── doc02. 하도급 공종별 작업내역서 ──────────────────────────
    "doc02": """\
도시가스 공사 「하도급 공종별 작업내역서」 검토.

이 서류는 여러 용역 항목 테이블(황물처단 용역, 비파괴 검사 용역, 공공측량 용역 등)로 구성됩니다.

[63A 인입공급관 표준 검토 기준]
- 공공측량 용역 테이블에만 데이터가 있어야 함 (다른 테이블은 비어있어야 함)
- 공공측량 용역 항목 — 인입분기 시추측량
  · 단위: 식, 수량: 1
  · 계약단가: 25,000원
  · 요율: 10%
  · 계: 27,500원 (= 25,000 × 1 × 1.1)

[사용자 입력 공사 정보 — 참고용]
{project_info}

[검토 항목 — 5개]
1. "공공측량 용역 항목 존재" — 인입분기 시추측량 행이 기재되어 있는가
2. "타 테이블 빈 상태" — 황물처단/비파괴 검사 등 다른 테이블이 비어있는가
3. "수량 1식 여부" — 수량 = 1, 단위 = 식
4. "계약단가 25,000원 여부" — 계약단가 = 25,000
5. "요율 10% 및 계 27,500원 여부" — 요율 = 10% 그리고 계 = 27,500 (콤마 무시)

[결과 규칙]
- 일치 → "결과": "OK", "추출값": 서류에서 추출한 실제 값
- 불일치 → "결과": "NG", "비고": "기준 X / 서류값 Y"
- 서류에서 확인 불가 → "결과": "SKIP"
- 판단 모호 → "결과": "WARN", "비고": 이유

응답은 반드시 JSON 배열만 (다른 설명/마크다운 금지). 한국어 키 사용.
[{"항목":"...","결과":"OK/NG/WARN/SKIP","추출값":"...","비고":"..."}]
""",

    # ── doc03. 기밀시험 데이터 ───────────────────────────────────
    "doc03": """\
도시가스 공사 「기밀시험 데이터」 검토.

서류에는 시간 경과(위쪽 → 아래쪽)에 따른 압력값이 기록되어 있습니다.

[사용자 입력 공사 정보 — 참고용]
{project_info}

[검토 항목 — 1개]
1. "압력 유지 여부"
   - 가장 위쪽(시작 시점) 압력과 가장 아래쪽(마지막 시점) 압력 비교
   - 마지막 압력 ≥ 시작 압력 → "결과": "OK" (압력 유지 — 누설 없음)
   - 마지막 압력 < 시작 압력 → "결과": "NG" (누설 의심)
   - 추출값: "시작 X → 종료 Y" (단위 포함, 예: "시작 5.0 kgf/cm² → 종료 5.0 kgf/cm²")
   - 압력값을 식별할 수 없으면 → "결과": "WARN", "비고": 이유

응답은 반드시 JSON 배열만 (다른 설명/마크다운 금지). 한국어 키 사용.
[{"항목":"압력 유지 여부","결과":"OK/NG/WARN/SKIP","추출값":"...","비고":"..."}]
""",

    # ── doc05. 교육일지/작업일보/스케치도면 ────────────────────
    # 이 서류는 파일이 여러 개 첨부됨. 각 파일별로 어떤 유형인지 식별.
    # 백엔드(reviewer.py)가 추출값을 보고 4가지 유형 모두 첨부됐는지 자동 집계함.
    # ⚠️ 추출값은 정확히 4가지 이름 중 하나여야 집계 인식됨 (다른 표현 사용 금지)
    "doc05": """\
도시가스 공사 「교육일지/작업일보/스케치도면」 첨부 파일 검토.

이 첨부 파일이 다음 4가지 유형 중 어느 것인지 식별하세요:
- 작업일보
- 안전보건환경 교육 및 TBM일지
- 교육 및 TBM 사진
- 스케치도면

[검토 항목 — 1개]
1. "서류 유형 식별"
   - 위 4가지 중 하나에 해당하면 → "결과": "OK"
     · 추출값은 반드시 다음 중 하나를 글자 그대로 사용 (다른 표현/축약 금지):
       "작업일보"
       "안전보건환경 교육 및 TBM일지"
       "교육 및 TBM 사진"
       "스케치도면"
   - 위 어디에도 해당 안 되면 → "결과": "WARN", "비고": 추정 내용
   - 두 유형이 섞여 있으면 가장 주된 것 1개만 식별

응답은 반드시 JSON 배열만 (다른 설명/마크다운 금지). 한국어 키 사용.
[{"항목":"서류 유형 식별","결과":"OK","추출값":"작업일보","비고":""}]
""",

    # ── doc06. 산업안전보건관리비 내역서 ──────────────────────
    # ⚠️ 멀티파일 통합 호출 — 첨부 파일들을 한 번에 함께 보고 서류 간 정합성 검증.
    # 6가지 유형 식별 + 6개 검증 룰 동시 적용.
    # 유형 식별 추출값은 정확히 6가지 이름 중 하나여야 함 (Python 집계 매칭용).
    "doc06": """\
도시가스 공사 「산업안전보건관리비 내역서」 검토.

첨부 파일은 다음 6가지 유형 중 일부 또는 전부입니다.
각 파일이 어느 유형인지 식별하고, 서류 간 정합성을 검증하세요.

[6가지 유형]
1. 산업안전보건관리비 사용내역서  ← 본 서류 제목 그대로 식별 (다른 명칭 사용 금지)
2. 항목 별 사용내역
3. 사진대지
4. 세금계산서
5. 거래명세서
6. 지급대장

[조건부 SKIP — 가장 먼저 확인]
사용내역서의 사용액이 0원이거나 "사용 없음"으로 표시된 경우:
  → 아래 한 줄만 반환하고 다른 검증 모두 생략:
  {"항목":"비용 사용 없음","결과":"OK","추출값":"사용내역서 합계 0원","비고":"증빙 서류 불필요"}

[검증 룰]

⚠️ 공통 비교 원칙 — 모든 룰에 적용
  • 두 값이 같으면 → 무조건 OK (예: 0=0, 95,000=95,000 모두 일치)
  • 두 값이 다르면 → NG
  • 각 룰은 단일 행으로 결과 반환 (룰 안의 항목별로 행 여러 개 만들지 말 것)
  • 절대 같은 값을 "불일치/NG"로 적지 말 것

룰 1: 사용내역서 내부 검증
  1-가. "공사진척도에 따른 기준금액" vs "최종 합계금액"
        → 같으면 OK, 다르면 NG (비고에 두 값 명시)
  1-나. 안전관리자 날인: 있으면 OK / 없으면 NG
  1-다. 현장소장 날인: 있으면 OK / 없으면 NG

룰 2: 사용내역서 ↔ 항목별 사용내역 (항목별 금액 7쌍 비교)
  2-가. 사용내역서의 항목 1~7 금액과, 항목별 사용내역의 항목 1~7 소계를
        같은 번호끼리 짝지어 비교 (총 7쌍).
        → 7쌍 모두 같은 숫자면 → 결과 OK, 추출값 "전 항목 일치"
        → 한 짝이라도 다르면 → 결과 NG, 추출값 "항목 N 불일치",
           비고 "사용내역서 X원 vs 항목별 Y원"

  [예시]
    사용내역서 [0, 0, 95000, 0, 0, 0, 0]
    항목별     [0, 0, 95000, 0, 0, 0, 0]
    → 결과 OK (모두 같음)

    사용내역서 [0, 0, 100000, 0, 0, 0, 0]
    항목별     [0, 0,  95000, 0, 0, 0, 0]
    → 결과 NG, 항목 3 불일치 (100,000원 vs 95,000원)

룰 3: 사진대지 ↔ 사용내역서/항목별 사용내역
  3-가. 사진대지의 공사명·품목 텍스트가 사용내역서·항목별 사용내역의
        공사명·품목과 일치하는지
        → 일치 OK / 불일치 NG (어느 부분이 다른지 비고에)

룰 4: 세금계산서 ↔ 항목별 사용내역
  4-가. 세금계산서의 일자/공급가액/품목 vs 항목별 사용내역의 대응 거래
        → 일자·금액·품목 모두 매핑 가능 → OK
        → 한 항목이라도 불일치 → NG (불일치 항목 비고에)
        · 품목은 카테고리 포함 관계도 일치로 인정
          (예: "안전화" ⊂ "개인보호구 구입비")

룰 5: 거래명세서 ↔ 세금계산서
  5-가. 거래명세서의 일자/공급가액/품목 vs 세금계산서
        → 모두 같은 값이면 OK
        → 다른 부분 있으면 NG (불일치 항목 비고에)

룰 6: 지급대장 ↔ 항목별 사용내역 — 수량 매칭 (조건부)
  - 항목별 사용내역에 "3. 개인보호구 ... 구입비" 항목이 있을 때만 적용. 없으면 SKIP.
  - 지급대장 양식은 업체마다 다름:
    · 표 형태: 보통 [번호 / 일자 / 성명 / 품목 컬럼들 / 수령확인(또는 서명) / 비고]
    · 품목 컬럼명 동적 (안전모, 안전화, 장갑, 보안경 등)
    · 항목 표시: 체크(✓, V, O) 또는 숫자(예: "2") 또는 빈 칸
    · 유효 행 판정: 수령확인/서명/도장이 있는 행만 유효
  6-가. 검증 방법 — 금액이 아니라 수량 비교:
    1) 항목별 사용내역에서 "3. 개인보호구 ... 구입비" 항목의 품목별 수량(EA/개) 추출
       · 표 구조 예: 사용일자 / 사용내역(품명) / 단가 / 수량(EA) / 금액
       · 예: "안전화 1EA / ₩95,000" → 안전화 수량 1개
    2) 지급대장의 유효 행(서명 있는 행)에서 품목별 수량 합산
       · 체크(✓, V, O) → 해당 품목 1개
       · 숫자(2, 3) → 해당 숫자만큼
       · 예: 안전화 컬럼 ✓ 1개 → 안전화 1개
    3) 두 수량을 비교 (금액 X, 수량 O):
       · 같으면 OK, 다르면 NG ("[품목명] 사용내역 N개 vs 지급대장 M개")

  [예시]
    사용내역: 안전화 1개
    지급대장: 안전화 1개 (✓ 1개)
    → 결과 OK (수량 1=1)

    사용내역: 안전화 5개
    지급대장: 안전화 3개 (✓ 3개)
    → 결과 NG, "안전화 사용내역 5개 vs 지급대장 3개"

  ⚠️ 절대 금액(원)과 수량(EA/개)을 혼동하지 말 것

[적용 안 되는 룰은 SKIP]
- 룰 적용에 필요한 서류가 첨부 안 되어 있으면 해당 룰을 "SKIP"으로 반환
  예: 사진대지 미첨부 → 룰 3 SKIP
- 식별 불가/판단 모호 → "WARN"

[응답 형식 — JSON 배열만, 한국어 키]
파일별 유형 식별 + 룰별 검증 결과를 각각 별도 항목으로 반환.

예시 (모든 파일 첨부 + 정상 케이스):
[
  {"항목":"파일 1 유형","결과":"OK","추출값":"산업안전보건관리비 사용내역서","비고":""},
  {"항목":"파일 2 유형","결과":"OK","추출값":"항목 별 사용내역","비고":""},
  {"항목":"파일 3 유형","결과":"OK","추출값":"사진대지","비고":""},
  {"항목":"파일 4 유형","결과":"OK","추출값":"세금계산서","비고":""},
  {"항목":"파일 5 유형","결과":"OK","추출값":"거래명세서","비고":""},
  {"항목":"파일 6 유형","결과":"OK","추출값":"지급대장","비고":""},
  {"항목":"[룰1-가] 기준금액 = 합계금액","결과":"OK","추출값":"1,200,000원","비고":"일치"},
  {"항목":"[룰1-나] 안전관리자 날인","결과":"OK","추출값":"있음","비고":""},
  {"항목":"[룰1-다] 현장소장 날인","결과":"OK","추출값":"있음","비고":""},
  {"항목":"[룰2-가] 항목별 금액 비교","결과":"OK","추출값":"전 항목 일치","비고":""},
  {"항목":"[룰3-가] 사진대지 텍스트 매칭","결과":"OK","추출값":"공사명·품목 일치","비고":""},
  {"항목":"[룰4-가] 세금계산서 ↔ 사용내역","결과":"OK","추출값":"일자·금액·품목 일치","비고":""},
  {"항목":"[룰5-가] 거래명세서 ↔ 세금계산서","결과":"OK","추출값":"일자·금액·품목 일치","비고":""},
  {"항목":"[룰6-가] 지급대장 수량 매칭","결과":"OK","추출값":"안전모 5/5 안전화 8/8","비고":""}
]

결과는 OK / NG / WARN / SKIP 만 사용.
다른 설명/마크다운 금지. JSON 배열만 응답.
""",
}
