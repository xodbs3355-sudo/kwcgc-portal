"""
서류별 검토 프롬프트 기본값.

이 파일은 "공식 기본값" — 관리자가 따로 설정 안 한 서류에 적용됩니다.
관리자 페이지에서 수정한 내용은 `data/prompts.json` (Railway: /data/prompts.json)에
저장되며 그쪽이 우선 적용됩니다.

프롬프트 내 `{project_info}` 자리에 사용자가 입력한 공사 정보가 자동 삽입됨.

수정 방법:
- 이 파일을 편집 → git push → Railway 자동 배포
- 관리자가 이미 저장한 프롬프트가 있는 서류는 영향 받지 않음
  (관리자가 "기본값으로 복원" 누르면 그 시점의 이 파일 내용을 가져감)

PROMPTS 딕셔너리에 없는 doc_id는 generic 기본 템플릿이 자동 적용됨.
"""

PROMPTS = {
    # ── doc01. 준공계 ────────────────────────────────────────────
    # ⚠️ 하이브리드 방식 — AI는 데이터 추출만, 비교/판정은 Python.
    # 비교는 reviewer.py 의 _doc01_apply_rules 가 담당.
    "doc01": """\
도시가스 공사 「준공계」 — 데이터 추출 단계.

⚠️ 중요: 비교/판정은 하지 마세요. 시스템(Python)이 별도로 처리합니다.
        당신의 역할은 첨부 파일에서 데이터를 정확히 추출하는 것뿐입니다.

[추출할 데이터 — JSON 객체]
{
  "공사명": <string, 서류에 표기된 공사명 그대로 (예: "춘천시 효자동 172-159번지 인입공급관")>,
  "준공일자": <string, YYYY-MM-DD 형식. 서류에 "26.05.18" 같이 적혀있어도 "2026-05-18" 로 변환>,
  "준공금액": <number, 원 단위 숫자만 (콤마/원 표시 제거). VAT 별도 표기면 VAT 미포함 금액>,
  "서명_날인": <boolean, 대표이사·현장소장 등의 서명 또는 도장(인)이 확인되면 true>
}

[추출 원칙]
- 추출 못 한 필드 → null
- 공사명은 표기된 그대로, 임의로 정정하지 말 것 (오타 보정 X)
- 일자는 YYYY-MM-DD 형식 변환. 안 되면 서류 표기 그대로 문자열로
- 금액은 콤마/원 표시 제거하고 순수 숫자만 (예: "3,203,910원" → 3203910)
- VAT 별도 명시면 그대로 VAT 미포함 금액
- 서명_날인: 도장 또는 친필 서명 중 하나라도 있으면 true

[사용자 입력 공사 정보 — 참고용 (비교 X)]
{project_info}

응답: JSON 객체만 (배열 X). 다른 설명/마크다운 금지.
""",

    # ── doc02. 하도급 공종별 작업내역서 ──────────────────────────
    "doc02": """\
도시가스 공사 「하도급 공종별 작업내역서」 검토.

이 서류는 여러 용역 항목 테이블(활관차단 용역, 비파괴 검사 용역, 공공측량 용역 등)로 구성됩니다.

[63A 인입공급관 표준 검토 기준]
- 공공측량 용역 테이블에만 데이터가 있어야 함 (다른 테이블은 비어있어야 함)
- 공공측량 용역 항목 — 인입분기 시추측량
  · 단위: 식, 수량: 1
  · 계약단가: 25,000원
  · 요율: 10%
  · 계: 27,500원 (= 25,000 × 1 × 1.1)

[사용자 입력 공사 정보 — 참고용]
{project_info}

[검토 항목 — 1개 (통합)]
항목명: "공공측량 용역항목 존재"
다음 조건(a~f)을 모두 만족해야 OK 판정:
  a) 공공측량 용역 테이블에 "인입분기 시추측량" 행이 기재되어 있을 것
  b) 단위 = "식", 수량 = 1
  c) 계약단가 = 25,000 (콤마/원 표시 무시)
  d) 요율 = 10%
  e) 계 = 27,500 (= 25,000 × 1 × 1.1, 콤마 무시)
  f) 활관차단/비파괴 검사 등 다른 테이블이 모두 비어있을 것
     ★ "비어있다" 판정 (엄격):
       - 품명 / 단위 / 수량 / 계약단가 / 계 칸이 모두 빈칸이거나 0이면 → 빈 테이블 (OK)
       - 요율("10%") 칸은 양식 인쇄 라벨이므로 무시. 요율만 적혀있고 나머지가 모두 비었으면 빈 테이블.
       - 가장 확실한 단일 기준: "계" 칸이 비었거나 0 → 빈 테이블.

[결과 판정]
- 모든 조건(a~f) 충족 → "결과": "OK"
  · "추출값": "인입분기 시추측량 / 식 1 / 단가 25,000 / 요율 10% / 계 27,500 / 타 테이블: 빈칸"
    (실제 값들로 정확히 채울 것)
  · "비고": "모든 항목 일치"
- 하나라도 어긋남 → "결과": "NG"
  · "추출값": 추출한 실제 값들 (가능한 만큼 같은 형식으로)
  · "비고": 어느 조건이 어긋났는지 구체적으로 명시
    예) "계약단가 불일치 (서류 30,000) — 그 외 항목은 일치"
    예) "타 테이블에 데이터 있음 (활관차단 테이블 품명 'A' 기재) — 그 외 일치"
- 서류에서 확인 불가 → "결과": "SKIP"
- 판단 모호 → "결과": "WARN", "비고": 이유

[항목명 작성 규칙 — 매우 중요]
- "항목" 키 값은 정확히 "공공측량 용역항목 존재" 로만 작성.
- "1.", "2." 등 번호 절대 붙이지 말 것.

응답은 반드시 JSON 배열만 (다른 설명/마크다운 금지). 한국어 키 사용.
배열에는 위 통합 항목 1개만 포함 (총 1개 요소).
[{"항목":"공공측량 용역항목 존재","결과":"OK/NG/WARN/SKIP","추출값":"...","비고":"..."}]
""",

    # ── doc03. 기밀시험 데이터 ───────────────────────────────────
    "doc03": """\
도시가스 공사 「기밀시험 데이터」 검토.

서류에는 시간 경과(위쪽 → 아래쪽)에 따른 압력값이 기록되어 있습니다.

[사용자 입력 공사 정보 — 참고용]
{project_info}

[검토 항목 — 1개]
1. "압력 유지 여부"
   - 가장 위쪽(시작 시점) 압력과 가장 아래쪽(마지막 시점) 압력 비교
   - 마지막 압력 ≥ 시작 압력 → "결과": "OK" (압력 유지 — 누설 없음)
   - 마지막 압력 < 시작 압력 → "결과": "NG" (누설 의심)
   - 추출값: "시작 X → 종료 Y" (단위 포함, 예: "시작 5.0 kgf/cm² → 종료 5.0 kgf/cm²")
   - 압력값을 식별할 수 없으면 → "결과": "WARN", "비고": 이유

응답은 반드시 JSON 배열만 (다른 설명/마크다운 금지). 한국어 키 사용.
[{"항목":"압력 유지 여부","결과":"OK/NG/WARN/SKIP","추출값":"...","비고":"..."}]
""",

    # ── doc05. 교육일지/작업일보/스케치도면 ────────────────────
    # 이 서류는 파일이 여러 개 첨부됨. 각 파일별로 어떤 유형인지 식별.
    # 백엔드(reviewer.py)가 추출값을 보고 4가지 유형 모두 첨부됐는지 자동 집계함.
    # ⚠️ 추출값은 정확히 4가지 이름 중 하나여야 집계 인식됨 (다른 표현 사용 금지)
    "doc05": """\
도시가스 공사 「교육일지/작업일보/스케치도면」 첨부 파일 검토.

이 첨부 파일이 다음 4가지 유형 중 어느 것인지 식별하세요:
- 작업일보
- 안전보건환경 교육 및 TBM일지
- 교육 및 TBM 사진
- 스케치도면

[검토 항목 — 1개]
1. "서류 유형 식별"
   - 위 4가지 중 하나에 해당하면 → "결과": "OK"
     · 추출값은 반드시 다음 중 하나를 글자 그대로 사용 (다른 표현/축약 금지):
       "작업일보"
       "안전보건환경 교육 및 TBM일지"
       "교육 및 TBM 사진"
       "스케치도면"
   - 위 어디에도 해당 안 되면 → "결과": "WARN", "비고": 추정 내용
   - 두 유형이 섞여 있으면 가장 주된 것 1개만 식별

응답은 반드시 JSON 배열만 (다른 설명/마크다운 금지). 한국어 키 사용.
[{"항목":"서류 유형 식별","결과":"OK","추출값":"작업일보","비고":""}]
""",

    # ── doc06. 산업안전보건관리비 내역서 ──────────────────────
    # ⚠️ 하이브리드 방식 — AI는 데이터 추출만, 비교/판정은 Python.
    # 멀티파일 호출 (6개 파일 함께 보고 구조화된 JSON 객체 반환).
    # 비교는 reviewer.py의 _doc06_apply_rules가 담당.
    "doc06": """\
도시가스 공사 「산업안전보건관리비 내역서」 — 데이터 추출 단계.

⚠️ 중요: 비교/판정은 하지 마세요. 시스템(Python)이 별도로 처리합니다.
        당신의 역할은 **첨부 파일에서 구조화된 데이터를 정확하게 추출**하는 것뿐입니다.

[6가지 서류 유형]
1. 산업안전보건관리비 사용내역서
2. 항목 별 사용내역
3. 사진대지
4. 세금계산서
5. 거래명세서
6. 지급대장

[조건부 종료 — 가장 먼저 확인]
사용내역서의 사용액이 0원이거나 "사용 없음"으로 표시된 경우:
  → 다음 JSON 객체만 반환하고 종료:
  {"비용_사용": false}

[추출할 데이터 구조 — JSON 객체]
{
  "비용_사용": true,

  "사용내역서": {
    "기준금액": <number, ★★ 「공사진척도에 따른 기준금액」 칸의 값만 추출 ★★>,
    "합계금액": <number, 최종 합계금액. 원 단위 숫자만>,
    "항목금액": [<number>, <number>, <number>, <number>, <number>, <number>, <number>],
                ↑ 항목 1~7의 각 금액 (0원도 0으로 그대로). 항상 7개 배열
    "안전관리자_날인": <boolean, 있으면 true>,
    "현장소장_날인": <boolean, 있으면 true>,
    "공사명": <string, 서류에 표기된 공사명 그대로>
  } | null,    ← 사용내역서 파일 없으면 null

  "항목별_사용내역": {
    "항목소계": [<number>, ..., <number>],   ← 항목 1~7의 소계 7개
    "보호구_품목": [
      {"품명": <string>, "수량": <number>}    ← 항목 3 "개인보호구 구입비"의 품목별 수량
    ]
  } | null,

  "사진대지": {
    "공사명_표기": <string>,
    "품목_표기": [<string>, ...]    ← 사진에 적힌 품목명 리스트
  } | null,

  "세금계산서": [    ← 여러 장이면 모두 (없으면 빈 배열 [])
    {
      "일자": <string, YYYY-MM-DD 또는 서류 표기 그대로>,
      "공급가액": <number, 원 단위>,
      "품목": <string, 품명>
    }
  ],

  "거래명세서": [    ← 형식은 세금계산서와 동일
    {"일자": <string>, "공급가액": <number>, "품목": <string>}
  ],

  "지급대장": [    ← 유효 행(서명 있는 행)에서 품목별 수량 합산
    {"품명": <string>, "수량": <number>}
  ],

  "식별_요약": [    ← 각 첨부 파일이 어떤 유형으로 식별됐는지 (디버그용)
    {"파일": <string, 파일명 또는 "파일 1">, "유형": <string, 6가지 중 하나 또는 "미식별">}
  ]
}

[★★ 사용내역서 "기준금액" 추출 주의사항 — 매우 중요 ★★]
사용내역서 상단 표에는 보통 3개 금액 칸이 있습니다:
  ① 공사금액         (공사 전체 도급액. 보통 수백만~수억 원의 큰 금액)
  ② 계상된 안전관리비 (공사금액 × 요율로 책정된 안전관리비 전체액)
  ③ 공사진척도에 따른 기준금액  ← **이 칸의 값만 "기준금액"으로 추출**

규칙:
- "기준금액" 필드에는 ③번 ("공사진척도에 따른 기준금액") 칸의 값만 넣을 것.
- ①번 (공사금액)을 절대 "기준금액" 으로 추출하지 마세요. 가장 흔한 오류입니다.
- 보통 ① > ② > ③ 순서로 크기가 작아집니다. 큰 숫자라고 무조건 기준금액이 아님.
- ③번 칸이 사용내역서에 없으면 null 로 둘 것 (추정 금지).

[지급대장 양식 변동성 안내]
- 표 형태: 보통 [번호 / 일자 / 성명 / 품목 컬럼들 / 수령확인(또는 서명) / 비고]
- 품목 컬럼명 동적 (안전모, 안전화, 장갑, 보안경 등)
- 항목 표시: 체크(✓, V, O) 또는 숫자(예: "2") 또는 빈 칸
- 유효 행 판정: 수령확인/서명/도장이 있는 행만 유효 (수량 합산 대상)
- 체크 표시 = 1개, 숫자(2,3) = 해당 숫자만큼

[추출 원칙]
- 추출 못 한 필드 → null 또는 빈 배열로 둠 (추정 금지)
- 0원도 그대로 0으로 기록 (생략 금지)
- 콤마/원 표시 제거하고 순수 숫자만
- 어떤 유형도 식별 안 된 파일은 "식별_요약"에 "유형":"미식별"로 기재

응답: JSON 객체만 (배열 X). 다른 설명/마크다운 금지. JSON 객체로만 응답.
""",
}
