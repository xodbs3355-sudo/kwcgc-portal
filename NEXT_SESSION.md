# 다음 세션 작업 메모

> 작성: 2026-06-18 / 작성자 메모 (다음 대화에서 이어서 진행)

## 배포 워크플로 (참고)
- 파이프라인: **푸시 → GitHub(`main`) → Railway 자동배포** (test-on-prod)
- Railway는 `main` 만 자동배포함. 작업 브랜치(`claude/...`)에만 푸시하면 prod 반영 안 됨 → **main 머지 필요**.
- 배포 상태 `ACTIVE` = 새 코드가 실제 사이트에 라이브. (BUILDING → DEPLOYING → ACTIVE)
- squash 머지 후엔 작업 브랜치를 `git reset --hard origin/main` 으로 맞춰둬야 다음 PR 충돌 안 남.
- 공사 저장은 서버가 아니라 **브라우저 IndexedDB**(`static/js/project_db.js`). 그 PC·그 브라우저에만 묶임.

## TODO (우선순위 미정, 원문 그대로)

### 1. 필수 서류 표시 + 검토 시작 차단
- 준공정보 입력 탭에서 **필수 서류 표시** (서류명 우측에, **모서리 둥근 가로 막대기(배지)** 형태 추천).
- 필수 서류가 첨부 안 되면 **「검토 시작」 버튼 눌러도 작동 안 되게** (validation).
- 관련 파일: `templates/upload.html` (서류 테이블 / 검토 시작 버튼), `static/js/app.js`.

### 2. 검토 결과 카드 디자인 변경
- 준공서류 검토 결과 탭의 **양호 / 보완 또는 확인 / 해당 없음** 카드 디자인 재검토.
- 목표: **"AI 제작 티"가 안 나게끔** 톤다운/정돈된 디자인.
- 관련 파일: `templates/result.html`, `static/css/style.css` (`.result-card` 등).

### 3. 최종 공사비 산식(별표 수식) 수정
- 최종 공사비 아래 별표로 된 산식을 **산업안전보건관리비 반영한 산식**으로 수정.
- 문제: **표시 폭이 좁아** 모든 항목을 어떻게 다 넣을지 레이아웃 고민 필요.
- 현재 힌트: `templates/result.html`의 "m당 연장 단가 + 일시점용료 + 영구신청수수료" 부분.

### 4. 서류별 프롬프트(검토 로직) 최종 완성
- 협력업체별 서류를 **전부 샘플링**해서 서류 항목별 프롬프트 최종 완성 (예: 준공계 등).
- 관련: `prompt_defaults.py`, `prompts_store.py`, `templates/admin_prompts.html`.

### 5. 프롬프트 없는 서류 항목 완성
- **화재위험작업허가서** 등 아예 검토 로직(프롬프트)이 없는 서류 항목 완성.
- (메모 원문 "하재위험작업허가서" → "화재위험작업허가서"로 추정)

### 6. AI 챗봇 실효성 검증
- 검토 결과 Q&A AI 챗봇 기능의 **실효성 검증**.
- 디벨롭 가능 범위 / 실제 유용성 판단 → 못 미더우면 **아예 제거** 고려.
- 관련 파일: `chat.py`, `templates/result.html`(챗 위젯), `templates/admin_prompts.html`.
